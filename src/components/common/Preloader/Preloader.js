import React from "react";
import spiner from "../../../assets/images/spin.svg";  

const Preloader = (props) => {
    return (
      <img src={spiner} alt="spiner"/>
    )
}

export default Preloader; 